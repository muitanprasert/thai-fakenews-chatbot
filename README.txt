Deploying:
$ git add .
$ git commit -am "commit message"
$ git push heroku master

# Next step: Perform similarity check with the article content. Right now the keywords are too overpowering and news of the same topics but with wildly different content result in the same page retrieved.